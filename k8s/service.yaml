apiVersion: v1
kind: Service

metadata:
  name: aws-devops-platform

spec:
  selector:
    app: aws-devops-platform

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: ClusterIP
